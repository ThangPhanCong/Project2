<div class="row">
  <div class="col-2 offset-sm-5">
    <%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),html: {method: :delete}) do |f| %>
      <%= f.submit t(".btn_unfollow"), class: "btn btn-outline-danger" %>
    <% end %>
  </div>
</div>
