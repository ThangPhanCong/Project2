<div class="row">
  <div class="col-2 offset-sm-5">
    <%= form_for current_user.active_relationships.build do |f| %>
      <div><%= hidden_field_tag :followed_id, @user.id %></div>
      <%= f.submit t(".btn_follow"), class: "btn btn-outline-primary" %>
    <% end %>
  </div>
</div>
